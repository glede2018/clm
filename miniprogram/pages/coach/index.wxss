.partner-header { margin: 8rpx 2rpx 26rpx; }
.title { font-size: 42rpx; font-weight: 700; margin-bottom: 8rpx; }
.invite-box { padding: 28rpx; }
.invite-button { width: 210rpx; min-width: 0; margin: 0; padding: 0 18rpx; border-radius: 18rpx; background: var(--green); color: #fff; font-size: 25rpx; line-height: 76rpx; }
.invite-button::after { border: 0; }
.bind-card { margin-top: 18rpx; padding: 28rpx; }
.bind-card .section-caption { display: block; margin-top: 8rpx; }
.code-row { width: 100%; min-width: 0; display: flex; align-items: center; gap: 12rpx; margin-top: 24rpx; }
.code-row input { flex: 0 0 340rpx; width: 340rpx; min-width: 0; height: 82rpx; padding: 0 18rpx; border-radius: 18rpx; background: var(--page); text-transform: uppercase; letter-spacing: 3rpx; font-size: 28rpx; }
.code-row button { flex: 0 0 170rpx; width: 170rpx; min-width: 0; height: 82rpx; margin: 0; padding: 0; border-radius: 18rpx; background: var(--green); color: #fff; font-size: 25rpx; line-height: 82rpx; }
.code-row button::after { border: 0; }
.privacy-note { margin: 22rpx 10rpx; color: var(--muted); font-size: 22rpx; line-height: 1.7; }
.partner-heading { display: flex; justify-content: space-between; margin: 34rpx 4rpx 16rpx; }
.partner-heading text:first-child { font-size: 30rpx; font-weight: 600; }
.partner-heading text:last-child { color: var(--muted); }
.partner-list { display: flex; flex-direction: column; gap: 14rpx; }
.partner-row { padding: 22rpx; display: grid; grid-template-columns: 70rpx 1fr auto; gap: 16rpx; align-items: center; }
.partner-avatar { width: 70rpx; height: 70rpx; border-radius: 50%; display: grid; place-items: center; background: var(--green); color: #fff; }
.partner-copy { display: flex; flex-direction: column; gap: 7rpx; }
.partner-copy text:first-child { font-weight: 600; }
.partner-copy text:last-child { color: var(--muted); font-size: 21rpx; }
.partner-actions { display: flex; align-items: center; gap: 12rpx; }
.partner-weight { font-size: 28rpx; font-weight: 600; }
.partner-weight text { margin-left: 3rpx; color: var(--muted); font-size: 18rpx; }
.partner-actions button { margin: 0; padding: 0 12rpx; background: transparent; color: var(--coral); font-size: 20rpx; line-height: 48rpx; }
.partner-actions button::after { border: 0; }
.empty-partners { padding: 70rpx 30rpx; text-align: center; color: var(--muted); }
.invite-mask { position: fixed; z-index: 1200; inset: 0; padding: 40rpx; display: flex; align-items: center; justify-content: center; background: rgba(67, 39, 31, .48); animation: mask-fade .2s ease both; }
.invite-dialog { position: relative; width: 100%; max-width: 610rpx; padding: 42rpx 32rpx 30rpx; border-radius: 30rpx; background: var(--surface); text-align: center; box-shadow: 0 24rpx 70rpx rgba(91,48,36,.22); animation: dialog-pop .3s cubic-bezier(.2,.8,.2,1) both; }
.dialog-close { position: absolute; top: 15rpx; right: 17rpx; width: 58rpx; height: 58rpx; display: flex; align-items: center; justify-content: center; border-radius: 50%; color: var(--muted); font-size: 42rpx; line-height: 1; }
.dialog-title { font-size: 34rpx; font-weight: 700; }
.dialog-caption { margin-top: 10rpx; color: var(--muted); font-size: 23rpx; }
.invite-code { margin: 34rpx 0; padding: 25rpx 18rpx; border: 2rpx dashed var(--green); border-radius: 20rpx; background: var(--surface-soft); color: var(--green-dark); font-size: 54rpx; font-weight: 750; letter-spacing: 12rpx; }
.copy-button { margin: 0; border-radius: 18rpx; background: var(--green); color: #fff; font-size: 27rpx; line-height: 82rpx; }
.copy-button::after { border: 0; }
@keyframes dialog-pop { from { opacity: 0; transform: scale(.88) translateY(20rpx); } to { opacity: 1; transform: scale(1) translateY(0); } }
@keyframes mask-fade { from { opacity: 0; } to { opacity: 1; } }
